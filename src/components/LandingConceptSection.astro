---
import { Image } from 'astro:assets';
import type { ConceptContent } from '../consts/landing-page';

interface Props {
	concept: ConceptContent;
}

const { concept } = Astro.props;
const conceptImageSizes =
	'(max-width: 640px) calc(100vw - 88px), (max-width: 900px) calc(100vw - 112px), calc(33vw - 115px)';
---

<section class="section section-concept">
	<p class="eyebrow">{concept.eyebrow}</p>
	<h2>
		{concept.titleLines[0]}
		<br />
		{concept.titleLines[1]}
	</h2>
	<p class="section-copy">
		{concept.copyLines[0]}
		<br />
		{concept.copyLines[1]}
	</p>

	<div class="card-grid">
		{
				concept.cards.map((card) => (
					<article class={`info-card concept-card ${card.tone}`}>
						<Image
							class="concept-card-image"
							src={card.image}
							alt={card.imageAlt}
							layout="constrained"
							widths={[320, 480, 600]}
							sizes={conceptImageSizes}
						/>
						<h3>{card.title}</h3>
						<p>{card.body}</p>
				</article>
			))
		}
	</div>
</section>
